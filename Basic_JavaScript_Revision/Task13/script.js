// Task
// Given an array of numbers, create a new result containing transformed values according to a rule of your choice.
const Numbers = [1,2,3,4,5];
const Squares = Numbers.map(n => n=n*n);
console.log(Numbers);
console.log(Squares);
